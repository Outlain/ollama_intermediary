# Validate with visudo -cf before installing as root:root mode 0440.
# No wildcard arguments, shell, reboot, GPU reset, daemon-reload or arbitrary unit.
ollama-intermediary-host ALL=(root) NOPASSWD: /usr/bin/systemctl restart ollama.service
